# ADR 0003 — Dois registries e OpenHands abaixo do orquestrador

Status: aceito.

Intelligence providers e code execution providers são capacidades distintas. OpenHands Agent Server executa tarefas e ACP agents, mas ProjectSpec, DAG, retries, eventos e lifecycle permanecem no control plane NEXORA.

